local plugin = {}

plugin.core = {"nvim-telescope/telescope.nvim"}

plugin.core.setup = {
    defaults = {
        mappings = {
            i = {

            }
        }
    },
    pickers = {},
    extensions={},

}    

plugin.core.config = function()
end     

plugin.mapping = function()
end   
