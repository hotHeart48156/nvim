local plugin = {}

plugin.core = {"hrsh7th/cmp-buffer"}

plugin.core.setup = function()
end

plugin.core.config=function()
--     require('cmp').setup({
--   sources = {
--     {
--       name = 'buffer',
--       option = {
--         -- Options go into this table
--       },
--     },
--   },
-- })
end

plugin.mapping=function()
end
return plugin
